import React from 'react';
import CopyrightTOS from './CopyrightTOS';

type Props = {};

const Footer = (props: Props) => {
  return (
    <div>
      <CopyrightTOS />
    </div>
  );
};

export default Footer;
